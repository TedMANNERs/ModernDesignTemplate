<UserControl x:Class="ModernDesignTemplate.Test2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Label Content="TestView2" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="Black" BorderThickness="2"/>
        <Button Height="30" Width="100" Margin="0,0,0,20" VerticalAlignment="Bottom" Content="Return" Command="{Binding ReturnHomeCommand}"/>
    </Grid>
</UserControl>
